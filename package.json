{
  "name": "yt-download-plus",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0",
  "description": "YT Download+ web app and manually installable browser extension.",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build && npm run extension:zip",
    "lint": "npm --workspace apps/web run lint",
    "extension:zip": "node scripts/build-extension-zip.mjs",
    "release:prepare": "node scripts/prepare-release.mjs"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
